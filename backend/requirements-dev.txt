-r requirements.txt

# The shared package's pytest plugin: the `dynamodb_tables`, `app_secret`
# and `frozen_clock` fixtures, and the moto backends they run on.
webbpulse[testing]==0.6.0
moto[dynamodb]==5.2.3
pytest==8.4.1
pytest-cov==6.2.1
httpx==0.28.1
psycopg2-binary==2.9.12
# Lint and format. Replaced flake8, black and isort in PR 4, because the org
# reusable workflow this repository now calls runs ruff.
ruff==0.16.6
